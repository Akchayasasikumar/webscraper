# Optional API Key for Anthropic Claude LLM semantic interpretation and candidate generation
# If not provided, the system automatically uses the built-in deterministic heuristic engines.
ANTHROPIC_API_KEY=

# Server Configuration
PORT=8000
HOST=0.0.0.0
